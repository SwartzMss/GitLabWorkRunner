# v1.0.9

## 改进

- stdout 日志改为 `tracing_appender::non_blocking` 后台写入，避免 stdout 写入阻塞 Tokio worker。
- AI Review 在 blocking HTTP task 返回 async 侧后新增短日志，便于确认 `spawn_blocking` 是否正常返回。
- AI Review raw response 的大段 preview 从 INFO 移到 DEBUG，降低大日志行卡住主流程的风险。
