# v1.0.10

## 改进

- AI Review HTTP worker 从 `tokio::task::spawn_blocking` 改为专用 `std::thread`。
- AI Review worker 通过 `tokio::sync::oneshot` 把结果传回 async 侧，绕开 blocking JoinHandle 返回路径。
- 新增 `AI review blocking HTTP worker result sent` 日志，用于区分 worker 内部卡住和 async 接收侧卡住。
