# v1.0.5

## 改进

- AI Review 的每次 HTTP 调用现在都会有更细的阶段日志，包含 attempt 编号、请求体大小、headers 返回、body 读取完成情况。
- AI Review 增加了内层 `tokio::time::timeout` 兜底，并在超时或可重试网络错误时自动重试一次。

## 说明

- `timeout_seconds` 仍然是总配置值。
- 单次 HTTP 尝试会使用约一半的超时时间，给重试留出预算。
