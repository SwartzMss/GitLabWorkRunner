# v1.0.0

第一个可用版本，基础 AI Review 功能已经可以使用。

## 主要功能

- 支持 GitLab Merge Request Webhook。
- 支持通过 MR 评论手动触发 `@ai-review`。
- 支持调用 OpenAI-compatible API 对 MR diff 做中文代码审查。
- 支持行级规则、脚本任务和 AI Review 的基础配置。
- 支持发布 GitLab MR Discussion。
- 补齐关键执行日志，方便定位 GitLab API、AI API 和评论发布流程。
