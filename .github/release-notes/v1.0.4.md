# v1.0.4

## 改进

- AI Review 现在会把请求体大小、prompt 大小、diff 截断状态和发送阶段日志打出来，方便定位大 diff 时卡在什么位置。
- AI Review 的失败路径会区分“发出请求前失败”和“读取响应体失败”，减少只看到卡住但不知道卡在哪一步的情况。

## 说明

- `max_diff_bytes` 仍然是字节上限，不是超时。
- `timeout_seconds` 仍然是单次 AI 请求的超时。
