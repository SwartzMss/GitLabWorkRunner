# v2.0.1

- 新增独立 `gitlab-work-runner-dashboard` 二进制，用于查看 review run、项目、MR、findings 和 comments 统计。
- Dashboard 复用主 `config.toml`，通过 `[dashboard].bind` 配置监听地址，并只读 `[storage].database_url` 指向的 SQLite 数据库。
- 修复脚本任务 findings 未汇总到 run 级统计的问题。
